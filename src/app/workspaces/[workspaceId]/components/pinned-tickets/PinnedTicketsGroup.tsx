import { SidebarGroup, SidebarGroupLabel } from "@/components/ui/sidebar";
import React from "react";

const PinnedTicketsGroup = () => {
  return (
    <SidebarGroup>
      <SidebarGroupLabel>Pinned Tickets</SidebarGroupLabel>
    </SidebarGroup>
  );
};

export default PinnedTicketsGroup;
